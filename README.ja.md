# Mycel

これは、マインドマップツールです。データはシステムのフォルダ、ファイルの構造をベースにしてます。

ファイルを縦方向の一覧として扱うのではなく、フォルダとファイルを接続されたノードとしてホワイトボード風のキャンバスに配置します。通常のシステムフォルダとファイルをそのまま使いながら、プロジェクト構造、文書群、知識ベースを地図のように整理し探索するためのツールです。

英語版は [README.md](README.md) です。

現在のバージョンは 0.2.6 です。

変更履歴は [CHANGELOG.md](CHANGELOG.md) を参照してください。

## 主な機能

- ホワイトボード風のキャンバス
- マップの周囲へ自由に移動できる広い仮想キャンバス
- ディレクトリ構造をマインドマップとして表示
- キャンバス上からフォルダとファイルを作成
- ドラッグ&ドロップでフォルダ間移動
- ファイルとフォルダを混在した順番で並び替え
- `.mycel/order.json` に並び順を保存
- `.mycel/colors.json` にユーザー指定色を保存
- `.mycel/previews.json` にプレビューの開閉状態とサイズを保存
- `.mycel/view.json` にキャンバス表示状態とウィンドウ状態を保存
- フォルダの折りたたみと展開
- テキストと Markdown のインラインプレビュー
- インラインプレビュー内の文字選択とコピー
- 選択した編集可能テキストファイルを直接編集し、自動保存する編集ペイン
- 表示/非表示を切り替えられる編集ペイン
- 左、右、下に配置を切り替えられる編集ペイン
- Ctrl + S で保存、Ctrl + マウスホイールで拡大/縮小できるテキストファイル用の内蔵エディタ
- ファイル間と次回起動後も維持されるエディタ文字サイズ
- 次回起動後も復元されるキャンバスの拡大率、表示位置、ウィンドウサイズ、最大化/全画面状態
- Markdown のレンダリングと折り返し表示
- 複数選択、複数プレビュー開閉、選択項目の削除
- 更新、名前変更、削除、色設定、作成、開くためのコンテキストメニュー
- `N` / `Shift + N` による選択位置基準のファイル/フォルダ作成
- `Ctrl + C` / `Ctrl + V` による選択項目のコピー
- `Tab` / `Shift + Tab` による表示中項目のキーボード移動
- `E` による編集ペインへのフォーカス移動
- `V` による選択ファイルのプレビュー開閉
- タッチパッドのピンチズーム
- マウスホイールズーム
- 空白キャンバスをドラッグして範囲を選択し、Enter でその範囲へズーム
- Ctrl + 0 による全体表示
- リロード、最大化、インライン名前変更、チートシート表示のキーボードショートカット
- 起動時は、引数なしならカレントディレクトリ、引数ありなら指定ディレクトリをルートとして表示
- `--version` によるバージョン表示と、タイトルバーへのバージョン表示

## 0.2.6 の更新内容

- 編集ペインを左、右、下に配置できるメニューを追加し、選択した位置を次回起動後も維持するように変更
- キャンバスの拡大率、表示位置、ウィンドウサイズ、最大化/全画面状態を `.mycel/view.json` に保存し、次回起動時に復元するように変更
- 起動時に Editor が ON の場合、編集ペインの表示状態を確実に同期するように修正
- 編集ペイン内の `Ctrl + S` で保存し、編集中ファイルへフォーカスを戻すように追加
- 編集ペイン内の `Ctrl + S` で、1行目の `Subject: filename.txt` に合わせてファイル名を変更し、その `Subject:` 行を本文から削除できるように追加
- `Subject:` / `Key:` で始まる行をインラインプレビューに表示しないように変更

## 0.2.5 の更新内容

- ルート配下のファイル数が多い場合、起動時はルート直下のフォルダまで表示し、それ以下を折りたたむように変更
- フォルダ選択時の `N` / `Shift + N` で `NewFile.txt` / `NewFolder` を作成するショートカットを追加
- ファイル選択時の `N` で、そのファイルと同じフォルダに `NewFile.txt` を作成するように変更
- `Ctrl + C` / `Ctrl + V` で選択ファイルやフォルダを同じ親フォルダへコピーできるように追加
- `E` で選択ファイルを右側編集ペインへフォーカスできるように追加
- `V` で選択ファイルのプレビューを開閉できるように追加し、複数選択時は全て開いていれば閉じ、1つでも閉じていれば開くように変更
- `Tab` / `Shift + Tab` で表示中の同階層項目を前後に移動できるように追加
- `N`、`Shift + N`、`V`、`Tab` 操作後にキャンバスのフォーカスと選択状態を維持するように修正
- 右側編集ペインで編集中のファイルがプレビュー表示中の場合、自動保存後にプレビューを更新するように変更
- 複数選択したファイルとフォルダのドラッグ、フォルダへのドロップ、削除の動作を改善
- ドラッグ中にドロップ先フォルダをハイライトするように追加

## 0.2.4 の更新内容

- 単一ファイルのコンテキストメニュー上部にプレビューを開く/閉じる操作を追加
- 複数選択のコンテキストメニューでも、プレビューを開く/閉じる操作を上部へ移動
- 色調整ダイアログを廃止し、コンテキストメニューの固定色パレットから色を選べるように変更
- 色表示を接続線ではなく、ファイル名、アイコン、プレビュー領域の淡い背景色として表示するように変更
- インラインプレビュー内の文字を選択してコピーできるように変更
- プレビュー本文のコンテキストメニューに「コピー」を追加
- キャンバスのズームを、Ctrl なしのマウスホイールで行うように変更
- `--version` でバージョンを表示して終了する機能と、タイトルバーのバージョン表示を追加
- 選択した編集可能テキストファイルを右側で直接編集し、自動保存する編集ペインを追加
- エディタ内の Ctrl + マウスホイールで変更した文字サイズを、他のファイルや次回セッションでも維持するように変更
- ツールバーの Editor ボタンと Ctrl + E で、右側編集ペインの表示/非表示を切り替えられるように変更

## 0.2.3 の更新内容

- ファイルのコンテキストメニューから開けるテキストファイル用の内蔵エディタを追加
- 内蔵エディタに Ctrl + S 保存、未保存変更の確認、外部変更時の上書き確認を追加
- 内蔵エディタで Ctrl + マウスホイールによる拡大/縮小に対応
- 仮想ホワイトボードキャンバスを広げ、ノードの外側へ自由に表示位置を移動できるように変更

## 0.2.2 の更新内容

- 選択されたファイルとフォルダの表示を統一し、アイコンと名前の範囲を枠と塗りで強調するように変更
- 通常状態のフォルダとルートフォルダの枠をなくし、選択中の項目とドロップ対象だけが目立つ表示に変更
- `.mycel/previews.json` にインラインプレビューの表示/非表示、幅、高さを保存
- 起動時、フォルダを開いた時、全体リロード時に保存済みのプレビュー状態を復元
- 何もない場所での右ボタンドラッグによるキャンバス移動を追加

## 0.2.1 の更新内容

- Ctrl + 左クリックによる複数選択を追加
- Shift + フォルダダブルクリックで、ダイアログなしに `NewFolder` を作成
- ファイルまたはフォルダを単一選択して F2 でインライン名前変更
- F5 の全体リロード、F11 の最大化/通常表示、`?` のチートシート表示を追加
- 起動時に、引数なしならカレントディレクトリ、引数ありなら指定ディレクトリを開くように修正

## ビルド

OS 別のビルドスクリプトを使えます。

macOS:

```sh
./scripts/build-mac.sh
```

Linux:

```sh
./scripts/build-linux.sh
```

Windows PowerShell:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-windows.ps1
```

Windows では、スクリプトが `C:\Qt` 配下にインストールされた Qt を自動検出できます。`C:\Qt\6.x\msvc2022_64` のような MSVC 版 Qt がある場合はそれを優先し、Visual Studio C++ 環境を取り込んで、起動時にターミナルウィンドウを開かない GUI subsystem の実行ファイルをビルドします。MSVC 版 Qt を使う場合の既定ビルドディレクトリは `build-windows-msvc` です。

手動で CMake を実行する場合:

```sh
cmake -S . -B build
cmake --build build
```

Qt 6 Widgets が必要です。

Qt Online Installer で Qt を入れていて `cmake` が `PATH` にない場合は、Qt 付属の CMake を使います。

```sh
/Users/fuk/Qt/Tools/CMake/CMake.app/Contents/bin/cmake -S . -B build -DCMAKE_PREFIX_PATH=/Users/fuk/Qt/6.11.1/macos
/Users/fuk/Qt/Tools/CMake/CMake.app/Contents/bin/cmake --build build
```

### ビルドスクリプトのオプション

macOS/Linux スクリプトは以下の環境変数を参照します。

- `BUILD_DIR`: ビルドディレクトリ。既定値は `build`
- `BUILD_TYPE`: CMake のビルド種別。既定値は `Release`
- `CMAKE_BIN`: CMake 実行ファイル。既定値は `cmake`
- `CMAKE_PREFIX_PATH`: Qt が自動検出されない場合の Qt インストールパス

例:

```sh
CMAKE_PREFIX_PATH=/path/to/Qt/6.x/gcc_64 ./scripts/build-linux.sh
```

Windows スクリプトは引数を指定できます。

```powershell
.\scripts\build-windows.ps1 -BuildType Release -CMakePrefixPath "C:\Qt\6.x\msvc2022_64"
```

MinGW や特定の generator を使う場合は `-Generator` を指定します。

```powershell
.\scripts\build-windows.ps1 -Generator "Ninja" -CMakePrefixPath "C:\Qt\6.x\mingw_64"
```

### Windows インストーラー

Windows のビルド済み出力を用意したあと、Inno Setup のインストーラーを作成します。

```powershell
.\scripts\package-windows-inno.ps1
```

インストーラー作成スクリプトは `build-windows-msvc` にある既存ファイルを使い、Mycel を再ビルドしません。Inno Setup の `ISCC.exe` が必要です。標準の場所にない場合は `-IsccPath` を指定してください。

## GitHub Actions

GitHub Actions 用の workflow を `.github/workflows/build.yml` に追加しています。

以下の環境で Mycel をビルドします。

- macOS
- Linux
- Windows

workflow では Qt 6 をインストールし、各OSのコンパイラ環境を設定した上で、OS別のビルドスクリプトを実行します。

## 起動

カレントディレクトリを開く:

```sh
./build/mycel
```

Windows MSVC ビルド:

```powershell
.\build-windows-msvc\mycel.exe
```

任意のディレクトリを開く:

```sh
./build/mycel /path/to/project
```

`.mycel` の metadata を読まず、作成もしないで起動する:

```sh
./build/mycel --no-mycel /path/to/project
```

バージョンを表示して終了する:

```sh
./build/mycel --version
```

通常起動時、ルートディレクトリに `.mycel` フォルダがない場合は、`.mycel` を作成するか、`--no-mycel` モードで開くか、別のルートディレクトリを選択するかを確認します。

`--no-mycel` モードでは、`.mycel/order.json`、`.mycel/colors.json`、`.mycel/previews.json`、`.mycel/view.json` を読み込まず、作成もしません。このモードでは並び替え、ノードの色変更、表示状態とウィンドウ状態の復元は無効になります。

## マウス操作

- 何もない場所をドラッグ: 範囲選択
- ?: チートシートを表示
- 範囲選択後に Enter: その範囲へズーム
- Ctrl + 0: 全体表示
- Ctrl + E: 編集ペインの表示/非表示
- ツールバーの Editor Place: 編集ペインを左、右、下へ移動
- E: 選択ファイルを編集ペインで編集
- 編集ペイン内の Ctrl + S: 保存して編集中ファイルへフォーカスを戻す。1行目が `Subject: filename.txt` の場合、保存後にファイル名を `filename.txt` へ変更し、その `Subject:` 行を本文から削除
- `Subject:` / `Key:` で始まる行はインラインプレビューに表示しない
- Tab: 同じ階層の次の表示項目へ移動
- Shift + Tab: 同じ階層の前の表示項目へ移動
- N: 選択フォルダ、または選択ファイルと同じフォルダに `NewFile.txt` を作成
- Shift + N: 選択フォルダに `NewFolder` を作成
- V: 選択ファイルのプレビュー表示/非表示
- Ctrl + C / Ctrl + V: 選択項目を同じ親フォルダへコピー
- F5: 全体をリロード
- F11: ウィンドウを最大化/通常表示
- Alt + 左ドラッグ、中ボタンドラッグ、または何もない場所で右ボタンドラッグ: キャンバス移動
- タッチパッドのピンチ: ズーム
- マウスホイール: ズーム
- タッチパッドスクロール: スクロール
- Ctrl + ノード左クリック: 複数選択に追加/解除
- Shift + ファイルクリック: プレビューを開く/閉じる
- Shift + フォルダクリック: 折りたたみ/展開
- フォルダをダブルクリック: そのフォルダに `NewFile.txt` を作成
- Shift + フォルダをダブルクリック: そのフォルダに `NewFolder` を作成
- ファイルをダブルクリック: OS の既定アプリで開く
- ファイルまたはフォルダを単一選択して F2: インラインで名前を変更
- 編集可能なテキストファイルを単一選択: 右側の編集ペインで直接編集
- エディタ内で Ctrl + マウスホイール: 文字サイズを変更し、他のファイルや次回起動後も維持
- OS からファイルまたはフォルダをフォルダノードへドロップ: そのフォルダへコピー
- ファイルまたはフォルダをドラッグ: 同じフォルダ内で並び替え
- ファイルまたはフォルダをフォルダへドロップ: そのフォルダへ移動
- プレビュー右下をドラッグ: プレビューサイズ変更
- プレビュー内の文字を選択して Ctrl + C、または右クリックの「コピー」でコピー
- ノードを右クリック: コンテキストメニューを表示

## コンテキストメニュー

単一ファイル:

- プレビューを開く
- プレビューを閉じる
- この項目を更新
- 全体を更新
- テキストファイルを編集
- 名前を変更
- 削除
- 固定色から色を選択/クリア
- 開く

複数ファイル選択:

- 選択ファイルのプレビューを開く
- 選択ファイルのプレビューを閉じる
- 選択範囲を更新
- 全体を更新
- 選択ファイルを削除

フォルダ:

- この項目を更新
- 全体を更新
- 折りたたみ/展開
- フォルダを作成
- ファイルを作成
- クリップボードの内容を貼り付け
- フォルダを削除
- 固定色から色を選択/クリア
- 開く

## サンプル

`Sample` ディレクトリには、業務改革を進めるためのマインドマップサンプルが含まれています。

```sh
./build/mycel Sample
```

現状把握、課題構造、改革テーマ、施策、KPI、ロードマップ、コミュニケーション、リスク管理、会議メモなどを、サブフォルダと Markdown メモで整理しています。

## 補足

Mycel は、開いたルートフォルダ配下に `.mycel` ディレクトリを作成します。ここには並び順、色設定、プレビュー状態など、Mycel 用のローカルメタデータが保存されます。
